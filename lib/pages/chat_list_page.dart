import 'package:flutter/material.dart';

class ChatListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('나의 채팅')),
      body: Center(child: Text('채팅 페이지')),
    );
  }
}
